# Expense Tracker

This is a simple command-line tool written in **Rust** to track your expenses. It helps you add expenses, view reports, and export your data to CSV files for easy tracking and sharing.

## Features
- **Add expenses** to various categories (e.g., Food, Transportation, Utilities).
- **View reports** of your expenses by date range or category.
- **Export expenses** to a CSV file for easy record-keeping and analysis.

## Installation

### Option 1: Download Binaries
Download the latest release for your platform:

- **Windows**: Download the Windows binary.
- **Linux**: Download the Linux binary (`.tar.gz`).

After downloading, extract the file and run the binary from the terminal.

### Option 2: Build from Source
If you prefer to build the tool from source, follow these steps:

1. **Install Rust**: If you don’t have Rust installed, download it from [rust-lang.org](https://www.rust-lang.org/tools/install).

2. **Clone the repository**:
   ```bash
    git clone https://github.com/aamnamuhammad/Project-Digital-Business.git
   cd Project-Digital-Business



